import { CustSearchPipePipe } from './cust-search-pipe.pipe';

describe('CustSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
